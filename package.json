{
  "name": "kouppi",
  "private": true,
  "packageManager": "pnpm@10.12.4",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.0.5"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}

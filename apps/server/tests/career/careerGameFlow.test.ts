/**
 * Career Game Flow Integration Tests
 *
 * Sprint 1 + Sprint 2 regression coverage is implemented in:
 * - careerQueueIntegration.test.ts (queue → match found)
 * - joinGuards.test.ts (join-after-start, reconnect tokens)
 * - careerFilter.test.ts (lobby exclusion)
 * - careerSecurity.test.ts (room identity)
 */

import { describe, it, expect } from "vitest";

describe("Career Game Flow Integration", () => {
  it("documents queue-based matchmaking regression coverage", () => {
    expect(true).toBe(true);
  });
});
